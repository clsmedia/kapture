### Get a single capture by id
# Paste a captureId from a webhook response (or from "List all captures") into
# http-client.env.json → dev → captureId, then run this request.
GET {{baseUrl}}/api/v1/captures/{{captureId}}
Authorization: Bearer {{apiToken}}
